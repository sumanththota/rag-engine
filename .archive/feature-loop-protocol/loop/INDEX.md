| id | title | outcome | iters | verif-reject | tokens | key learning | link |
|----|-------|---------|-------|---------------|--------|--------------|------|
| 9 | password auth | merged | unreliable (see retro) | 2 | not captured | full-suite + 3x repeat run caught a regression and a ~25%-flaky test the implementer's own green suite missed | .loop/9/retro.md |
| 11 | persist threads | merged | 7 implementer passes (16 journal entries) | 6 | not captured | store-vs-HTTP test gap and a JS/backend split hid unmet criteria behind a green suite; a shared bounds-check function replaced two rounds of per-route patches | .loop/11/retro.md |
| 10 | google oauth | merged | 4 implementer passes (10 journal entries) | 3 | ~680k subagent tokens (harness-reported; orchestrator excluded) | mocks at the wrong seam hid a different real defect each round; two rounds traced to guidance errors in the manifest itself (parse_id_token, missing openid scope); a per-clause mutation kill-list belongs in the manifest up front | .loop/10/retro.md |
| 12 | migrate pre-login threads on first login | merged | 4 implementer rounds (round 4 a human-approved bounded round), 1 mid-ticket FLP v3 rescaffold | 3 (2 NEEDS_WORK/STOP, 1 PASS) | ~128k subagent tokens for the final verifier round alone; earlier rounds not captured | same hollow-assertion failure class survived 2 patch attempts by moving from comment- to docstring-satisfiable text-match, only closed by switching to real-execution/real-MVCC tests; verify-branch staleness recurred a 5th time across #11+#12; orchestrator-created worktrees don't get `.worktreeinclude`'s `.env` copy | .loop/12/retro.md |
| 18 | login UI: form + submit calling afterLogin() | merged | 1 implementer round + 2 orchestrator-direct fix rounds (region boundary, then a 2-layer hollow-assertion fix) | 2 (1 NEEDS_WORK, 1 PASS) | not captured | a hollow-assertion fix can itself be hollow one layer deeper — the orchestrator's own fix for round 1's finding introduced a content-level hollow guard, caught only by re-running the same mutation method against its own patch; GitHub label drifted from LOOP.md's state field again (agent:in-progress left stuck alongside agent:verified) | .loop/18/retro.md |
